# 变更记录

> 状态：进行中

## 2026-09-11

- 用户确认既有讨论方案并要求通过 spec-driven-development 进行设计；整理 project-context.md、requirements.md，需求标记为已确认。
- 生成 design.md 草稿：开机初始化后自动睡眠、CPU 三档、期限与事件等待、键盘/显示独立状态，以及 SDK/BLE 验证门槛。
- 当前为 Phase 2 设计审阅，尚未创建正式任务清单，未修改生产代码或依赖，未运行新增构建或硬件测试。

- 用户回复“继续”，确认 Phase 2 技术设计；design.md 状态更新为已确认。
- 生成 Phase 3 tasks.md 草稿，共 13 项任务；CPU/期限逻辑可先推进，睡眠集成依赖 SDK/BLE 实测门槛。当前等待任务清单确认，未修改生产代码、依赖或暂存区。

- 用户要求继续 20260911_cpu_light_sleep，确认 Phase 3 任务清单，进入 Phase 4。
- 完成 task-01：新增 sdk-evaluation.md，记录实际构建依赖、SDK 配置、产物摘要、键盘接入候选、实验步骤及回退。纠正项目上下文：实际使用 Arduino 2.0.16，之前的 2.0.17 是另一个安装包。
- 现有目标增量构建退出码 0，RAM 75164 B、Flash 2099449 B；未执行新增逻辑测试、实验固件、烧录或实机验收。未修改生产代码、默认依赖和暂存区。task-07 未完成，下一项为 task-02。

- 用户指定执行 task-02、task-03、task-04，本轮实现范围止于这三项。
- 完成 task-02：新增唯一应用改频入口 PowerAdapter 与非 PM 平台实现，限制 80/160/240 MHz，核对实际频率并在失败时恢复；恢复失败返回真实频率及独立错误。应用启动频率设为 160 MHz；自动睡眠能力仍不可用。平台替身 2 个用例成功，目标板编译链接真实接口。
- 完成 task-03：新增独立 power.json 配置与严格解析，复用 ConfigFileStore，缺失/损坏使用默认且启动不写盘；应用失败不保存，写入/回读失败保留实际值，读失败不覆盖文件，同值重试不重复写。配置替身 4 个用例成功，Flash 实机持久化待确认。
- 完成 task-04：设置首页追加 CPU频率，复用四行滚动与行内反馈；自动保存，Enter 重试保留目标，返回/重入保留错误；实际 setup 在 BLE 启动前应用 CPU 配置。受影响 8 个 native 套件共 32 个用例成功，目标板构建退出码 0，RAM 75212 B、Flash 2104293 B。
- 新增 acceptance.md，分别记录自动化和人工检查项。构建首次遇全局 platforms.lock 沙箱权限，按权限机制重试完成，未升级依赖或修改缓存来规避。未烧录、未执行 uploadfs、未创建提交，未开展 task-05 或睡眠集成。
- 完成 task-05：ScheduledTaskService 新增只读 `nextWaitMs(now)`，统一使用回绕安全 elapsed 计算，不触发任务、不改变时间戳；新增 1 个测试场景后共 5 个调度测试成功。未改动任务触发语义，未开展 task-06 或睡眠集成。
- 用户反馈测试完成，要求检查代码清晰性与冗余。复核 task-02～task-05 的平台、配置、设置及调度实现：保留实际频率与待重试目标的必要区分，将首页五层三元表达式改为直接页面映射，补清 nextWaitMs 空值/零值契约。相关页面、实际主循环和调度 3 个 native 套件共 16 个用例成功，差异空白检查无错误。本轮整理未重新构建或烧录目标板；用户未逐项列明实机结果，验收矩阵仍保留原记录。
- 按用户要求清理规划：firmware-power-saving-plan.md 移除已实现项并只保留后续工作；删除已由本规格覆盖的 firmware-cpu-light-sleep-plan.md，项目上下文改链至正式需求/设计。核对源码仍为 delay(10) 且自动睡眠返回不支持，故 task-06～13 保持未完成；删除重复讨论稿不代表 Light-sleep 已实现。
