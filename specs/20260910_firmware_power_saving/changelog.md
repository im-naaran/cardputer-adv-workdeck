# 固件第一期省电：变更记录

> 状态：草稿

## 2026-09-10

- Phase 0 完成：核对固件、显示配置、键盘输入、调度、Wi-Fi 收尾与外设初始化，生成项目上下文。
- Phase 1 生成需求草稿，待用户确认后进入技术设计。
- 相比历史讨论稿，统一亮屏/息屏每 60 秒采样，取消唤醒补采；纳入用户确认的首键只点亮屏幕、显示与后台分离及 Wi-Fi 关闭失败自动收尾。
- 本阶段未修改生产代码，未进行设备或功耗验收。
- 用户回复 `approve`，Phase 1 需求规格已确认。
- Phase 2 生成技术设计草稿：确定统一显示策略、独立电量任务、显示配置兼容和 Wi-Fi 最多 3 次自动关闭重试；记录当前外设库接口的限制。待确认后拆解任务。
- 用户回复“继续”，Phase 2 技术设计已确认。
- Phase 3 生成 10 项任务草稿，分别记录依赖、修改范围、验证与回滚边界；实机与功耗验收独立列项，所有任务均未执行。
- 补充核验：当前本机 TCA8418 驱动每次 update 只读取一个 FIFO 事件，可沿用现有键盘更新观察连续按下/释放，无需修改第三方依赖。

- 用户回复“继续”，Phase 3 任务清单已确认，进入 Phase 4 实现。
- task-01 完成：显示配置新增自动息屏时长、旧文件兼容和严格校验；定向测试 3/3。
- task-02 完成：物理活动报告、唤醒与释放门控状态机；键盘/路由/策略定向用例执行成功。
- task-03 完成：电池读取迁移到独立适配器和缓存，共享调度注册 60 秒任务，显示只读快照；相关定向检查执行成功。
- task-04 完成：自动息屏设置、完整快照保存和保存失败重试；控制器/页面定向用例执行成功。
- task-05 完成：实际主循环接入唤醒门控、背光控制及息屏绘制过滤，相关回归 21/21；实机结果仍待验收。
- task-06 完成：可见时间比较与静态周期过滤，定向检查 11/11。
- task-07 完成：每请求额外最多 3 次、间隔至少 1 秒的 Wi-Fi 收尾，自动/手动状态反馈同步；定向检查 30/30。
- task-08 完成软件范围：禁用闲置 IMU/音频初始化，补充外设证据和验收表；目标固件构建成功，真实外设状态留待 task-10。
- task-09 完成：33 个 native 套件共 135/135 succeeded，目标板构建 SUCCESS；同步操作文档和实机清单。自动化与构建不作为硬件验收。
- task-10 待实机：未刷机，未取得前后功耗及实际设备交互证据，保持未完成。

- 审查修复：字段名按实际长度校验，拒绝内嵌 NUL 的未知字段并补充配置保存不变的回归；电量有效值校验统一在 BatteryService，删除未使用的状态访问接口。
- 规格自包含：将必要的省电边界写入项目上下文，设置模块依据改为正式需求，移除规格对临时文档的引用。

## 2026-09-11

- 审查修复验证完成：配置、电量、息屏策略、设置控制器和实际主循环 5 个定向套件共 17/17 succeeded；目标板构建 SUCCESS，RAM 75,164 字节、Flash 2,099,449 字节。
- 规格本地链接目标均在版本索引内，已无临时文档引用；实机验收仍待完成。
