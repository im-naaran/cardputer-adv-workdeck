# 第一期省电：验收记录

> 状态：待实机验收

日期：2026-09-10。软件实现与自动化记录见 [任务清单](tasks.md)。本次未刷机、未采集真实电流、未确认设备交互；不得将以下预期填写为实际结果。

## 自动化与构建证据

- 定向用例已覆盖配置兼容、物理键盘活动、首键门控、缓存采样、可见时间刷新和 Wi-Fi 重试。
- 完整 native 回归：`pio test -d firmware -e native`，33 套件，135/135 succeeded，耗时 40.232 秒；包含实际主循环中脚本/剪贴板首键不执行、下一次正常执行及息屏动作超时的覆盖。
- 目标板：`pio run -d firmware -e m5stack-cardputer-adv` 构建成功，RAM 75,164 / 327,680 字节，Flash 2,099,449 / 3,342,336 字节。
- 平台 `espressif32@6.7.0`，Arduino ESP32 2.0.16；M5Cardputer 声明 tag `1.2.0`，本次解析为 `1.1.1+sha.2d4fa66`（提交 `2d4fa6646e4e5b47e0af96214b003aa7b15b8d81`），M5Unified 0.2.21，M5GFX 0.2.28，ArduinoJson 7.4.3。未升级依赖。
- 审查修复复核（2026-09-11 记录）：5 个定向套件（test_display_config、test_battery_service、test_screen_power、test_power_integration、test_settings_controller）共 17/17 succeeded，耗时 6.783 秒；目标板重新构建 SUCCESS，耗时 12.487 秒，RAM/Flash 使用量同上。新增用例确认含内嵌 NUL 的字段名被拒绝且不改写原配置；未重跑完整套件。
- 测试替身可检查实际主循环中的读取/绘制次数和请求状态，不能证明物理背光、器件断电、真实 BLE 可靠性或续航。

## 实机检查

首次刷入改造固件前保存原固件、兼容显示配置和功耗基线；使用普通应用刷写，不执行 `uploadfs`。本节全部实际结果尚未提供。

| 编号 | 入口与步骤 | 预期 | 实际结果 |
| --- | --- | --- | --- |
| H-01 | 旧版亮度配置启动；Fn+4 → 自动息屏 | 原亮度保留，缺省 10 分钟，四行首页可读 | 待验收 |
| H-02 | 五档逐项选择，Tab/方向键/返回，再重启 | 每档正确保存；永不不息屏，调整时长不覆盖亮度 | 待验收 |
| H-03 | 默认档无按键等待完整 10 分钟；另测长按后释放 | 10 分钟边界熄灭，按住不熄灭，释放后重新计时 | 待验收 |
| H-04 | 各页面用普通键、Fn/Shift/Ctrl/Alt/Opt、Fn+数字、Alt+字母及长按唤醒 | 首次只点亮；无输入/查询/脚本/粘贴/切页；全部释放后的下一次正常 | 待验收 |
| H-05 | 设置编辑中息屏；后台有新数据后唤醒 | 草稿与页面保留；最新画面一次显示，恢复用户亮度 | 待验收 |
| H-06 | 息屏时查询、授时、动作超时、断连/重连 | 后台继续，不自动亮屏；唤醒显示当前状态 | 待验收 |
| H-07 | 连续按键、消息和反复唤醒，观察电池读取计数 | 启动一次，随后每 60 秒；无额外按键读取 | 待验收 |
| H-08 | Codex 页跨分钟、滚动和切页；静态页停留 | 可见时间正常变化，静态分钟检查不推帧 | 待验收 |
| H-09 | 设置 → Wi-Fi → 扫描/测试；进行中息屏，结束后看网络信息 | 请求正常收尾并关闭驱动，BLE 保持可用 | 待验收 |
| H-10 | 可控测试替身注入关闭失败与存储失败 | 自动重试最多 3 次、失败可手动处理；设置显示当前生效/保存失败 | 自动化已覆盖；设备异常提示待验收 |
| H-11 | 外设控制前后分别冷启动与 MCU 热重启 | 键盘、显示恢复和配置存储正常；记录器件真实状态 | 待验收 |

H-07 可使用临时计数诊断检查读取节奏，测量功耗时关闭高频诊断；不要将 ADC 估算百分比或充电接口当作精确电量仪表。H-10 无需人为破坏真实驱动，异常行为以可控注入和提示核对为主。

## 外设状态审计

代码依据来自本次实际解析的依赖，不修改依赖缓存。

| 对象 | 静态依据与已实施动作 | 剩余限制 | 实机状态 |
| --- | --- | --- | --- |
| BMI270 | M5Unified 默认会尝试初始化，BMI270 begin 禁用节能并加载配置；现设 internal_imu=false，external_imu=false | 当前 BMI270 未实现有效 sleep；MCU 复位不一定复位独立供电器件 | 冷启动/热重启均待验收 |
| 麦克风/编解码器 | 当前应用不录音，现设 internal_mic=false | 禁止初始化不证明模拟电路已断电 | 待验收 |
| 扬声器/功放 | 当前应用不播放，现设 internal_spk=false；ADV 扬声器关闭回调寄存器表为空 | 不依赖 Speaker.end() 宣称硬件关闭 | 待验收 |
| LED/红外 | 应用未启用相应业务，普通系统 LED 配置默认 0 | 不能据此推断所有板载灯状态；没有添加 GPIO 写入 | 待验收 |
| SD/外接器件 | 应用未启动相关业务，保持未初始化 | 外接器件自行耗电需单独测量 | 待验收 |
| 外接供电 | 未盲目修改 output_power 或公共供电域 | 控制域与实际剩余功耗尚未实测 | 待验收 |
| 键盘/共享 I²C/显示/Flash | 保留键盘启用与共用资源，不调用总线 end | 需核验禁用 IMU/音频后唤醒与存储正常 | 待验收 |

若测出仍活动且可安全关闭的器件，应补充该器件设计后处理；R-06 不提前宣布全部验收。不能软件断电的器件记录限制与剩余功耗。

## 功耗对照

测量前填写：原固件标识、改造固件标识、亮度、电池电量区间、主机/信号、测量仪器、采样时长、测量点（电池侧或 USB 侧）。目前均未采集，不以 USB 供电场景直接推算电池续航。

| 场景 | 原固件平均/峰值电流 | 改造后平均/峰值电流 | 操作耗时/备注 |
| --- | --- | --- | --- |
| 亮屏 BLE 在线空闲 | 待测 | 待测 | 固定同档亮度 |
| BLE 在线空闲，改造后自动息屏 | 待测（原版保持亮屏） | 待测（息屏） | 相同空闲时段比较 |
| 未连接广播 | 待测 | 待测 | 明确亮屏/息屏状态 |
| Wi-Fi 扫描/测试后空闲 | 待测 | 待测 | 确认驱动关闭 |
| 禁用闲置外设前后 | 待测 | 待测 | 其余策略保持相同，逐变量对照 |

结论：待实机数据和用户确认；task-10 保持未完成。
