> 状态：待人工验收。task-08～10 已有代码、自动化和文档证据；task-11 未完成。

# 剪贴板与直接粘贴验收

## 环境记录

日期、验收人、macOS 版本、启动宿主、权限状态：待填写。ADV 串口标识、实际刷写版本、桌面版本、目标应用版本：待填写。未刷写设备、未操作真实剪贴板或前台按键。

先按 [根说明](../../README.md) 配套更新协议 v2 固件与桌面服务。无需为本功能 uploadfs；保留既有配置。权限步骤见 [桌面说明](../../desktop/README.md#剪贴板配置与权限)。

## 可复制验收配置

完整 JSON：[acceptance-config.json](acceptance-config.json)。在 desktop 目录启动：

```sh
uv run adv-workdeck --config ../specs/20260907_clipboard_direct_paste/acceptance-config.json
```

包含 17 条剪贴板和 2 条脚本，剪贴板跨 3 页（8/8/1）；脚本命令为 true，无业务副作用。Alt+G 应归首项 script.first，clipboard.item1 不显示该快捷键但列表可粘贴；Alt+P 应归 clipboard.item17，script.later 不显示该键但列表可执行。配置第 1～3 条文本分别包含中文/emoji/换行/Tab/首尾空格、RTF 头和 EPS 头。常规默认配置的 npm install 文本不带提交换行。

## 人工记录

所有“实际/证据”目前为待执行，需记录日期、环境及实际差异，不能用自动化结果代填。

| 编号 | 入口和步骤 | 预期 | 实际/证据 |
| --- | --- | --- | --- |
| 01 | 更新两端后连接；单独用旧版 hello 对照 | v2 就绪，v1 保持等待且不重放 | 待执行 |
| 02 | 离线 Fn+3，再连接；在 Codex 页连接后 Fn+3 | 离线等待；首次进入加载当前目录 | 待执行 |
| 03 | Fn+3 上下走完 17 项，再返回第 1 项 | 8/8/1 分页，四行高亮、计数正确，边界停止 | 待执行 |
| 04 | 在两页分别滚动，Fn+2/Fn+3 来回切换 | 选择与滚动独立保留；中文名称裁剪不挤快捷提示 | 待执行 |
| 05 | 空 actions、剪贴板能力缺失分别连接 | 空目录/能力不可用文案不同，不执行旧项 | 待执行 |
| 06 | 目录加载期间断开或受控延迟，失败后 Enter | 加载中不执行；失败 Enter 只重读，再按才粘贴 | 待执行 |
| 07 | 在纯文本编辑器聚焦后粘贴第 1～3 项 | 中文、emoji、Tab、换行、空格及 RTF/EPS 头均为原文 | 待执行 |
| 08 | 浏览器多行输入框重复上述操作 | 原文插入，无提交、无抢焦点 | 待执行 |
| 09 | IDE 编辑区重复上述操作 | 原文插入；记录编辑器自身格式处理差异 | 待执行 |
| 10 | 粘贴默认 npm install 到普通输入框 | 不附加 Enter，不主动提交；新剪贴板保留 | 待执行 |
| 11 | 四页各按普通 g/p、Alt+G、Alt+P；选中重复键后项 Enter | 普通字母不执行；跨类型首项归属正确；后项列表可执行 | 待执行 |
| 12 | 长按与释放重按；执行中快速触发脚本和剪贴板 | 长按不重复，忙时拒绝不排队；完成后可再次执行 | 待执行 |
| 13 | 粘贴进行时切页并查询 Codex/授时 | 导航和查询正常；全局反馈跨页显示，终态约 3 秒消退 | 待执行 |
| 14 | 未授权启动，触发；授权后重新聚焦并手动触发 | 权限错误不冒充成功；无自动补发，重试正常 | 待执行 |
| 15 | 受控注入写入失败、粘贴阶段失败 | 写入失败无粘贴；部分失败提示文本已复制 | 待执行，需可控故障环境 |
| 16 | 受控超时/断线；先观察输入框再重连 | 结果未确认、不重放；副作用不承诺回滚 | 待执行 |
| 17 | 两份不同电脑配置连接，注入旧响应 | 两页旧目录、执行与反馈清空，旧响应无效 | 待执行 |
| 18 | 设置编辑、四模块方向映射、Fn+Enter、Wi-Fi 测试和亮度 | 原输入优先级和设置行为保留 | 待执行 |
| 19 | 切换焦点或使用拒绝粘贴的控件 | “已发送粘贴”与实际插入分别记录，不宣称目标已消费 | 待执行 |

用户确认以上真实结果后才可勾选 task-11；无法复现的故障项保持未验收并记录原因。

## 自动化记录（2026-09-09）

- 桌面：`UV_OFFLINE=1 uv run --locked --no-sync python -m pytest -q`，330 passed in 2.06s。沿用已有虚拟环境，不同步或下载依赖。
- 固件：`~/.platformio/penv/bin/pio test -d firmware -e native`，29 套件、121/121，30.520 秒。含实际 main.cpp 的双目录接入与既有设置集成，以及两阶段等待取消回归。
- 目标：`~/.platformio/penv/bin/pio run -d firmware -e m5stack-cardputer-adv`，SUCCESS，30.879 秒；RAM 74,764/327,680，Flash 2,095,293/3,342,336。
- 首次调用受沙箱缓存访问限制，使用既有授权在沙箱外执行；没有安装/升级依赖或修改锁文件。未刷写、未验证真实 BLE、CJK 像素、权限或焦点输入。

## 用户运行反馈（2026-09-09）

用户报告 17:06:42 的 actions.shortcut.execute 响应 ERROR，execId=0000000000000008、durationMs=1409；可写入剪贴板，未能直接粘贴。启动宿主、权限状态与具体 reason 尚待确认，实际直接粘贴未验收。已补充安全摘要日志用于下次复现，不能以该诊断改动视为故障已修复。
