> 状态：已确认

# 脚本模块与全局快捷键：任务拆解

用户已确认任务拆解，2026-09-06 继续授权 task-08～09；task-01～08 已完成实现及自动化记录，task-09 的操作文档与验收材料已准备，待真实设备结果。脚本总条数不设业务上限；每页 8 条仅限制传输与设备缓存。

按编号顺序实施；单个任务可独立验证和回退其变更，已有下游集成时先逆序撤回依赖任务。阶段内新增能力先完成对应测试，再接入主循环。不升级依赖、不刷写设备；自动化结果与用户真机验收分别记录。

## task-01：共享协议与分页契约

- [x] task-01: 共享协议与分页契约
- 追踪需求：R-01、R-02、R-04、R-07。
- 依赖任务：无。
- 修改范围：protocol/README.md、fixtures；双端 protocol_constants 和消息编解码及对应测试。
- 公共能力处理：扩展现有 JSONL 契约和 codec；不新建传输层。
- 代码注释要求：固定能力、外层协议动作与实际脚本 ID 的区别，分页校验边界。
- 完成标准：定义 actions.list、scripts.execute、actions.shortcut.execute；完成分页/执行数据结构、编码解码及兼容契约，不改变现有动作。
- 自动化验证：双端 fixtures 验证空页、完整页、末页、重复键、错误分页字段、错误执行结果、未知字段；最坏转义和最长 execId 下整帧不超过 4096 字节；旧 hello/Codex/时间用例回归。
- 人工验证关注点：不涉及新 UI；最终联调检查真实 BLE 分片。
- 待确认问题：无。

task-01 验证记录：桌面协议相关 58 项、native 协议 10 项自动化断言完成；真实 BLE 分片待后续集成验收。

## task-02：桌面脚本配置与分页目录

- [x] task-02: 桌面脚本配置与分页目录
- 追踪需求：R-01、R-02、R-04。
- 依赖任务：task-01。
- 修改范围：desktop/config.json、config.py、application/script_module.py、目录与配置测试。
- 公共能力处理：扩展既有 actions 校验和错误隔离；目录服务共用全量有效动作。
- 代码注释要求：重复 key 合法、首项 effectiveKey 与总量不限、分页不是条数上限。
- 完成标准：默认 Google 条目可加载；不限制总条数；按配置顺序输出每页最多 8 条和分页元数据；跨页重复键 effectiveKey 正确。
- 自动化验证：构造 0、1、8、9、17、101 条并遍历所有页面，确认无遗漏/重复且末项存在；混合禁用/非法项、重复 key/ID、大小写、offset 和帧大小测试。
- 人工验证关注点：文档样例可读；真实脚本列表展示在 task-08/09 验收。
- 待确认问题：无。

task-02 验证记录：配置与分页相关 45 项自动化断言完成，含 0/1/8/9/17/101 条及跨页重复键；未运行默认 Google 动作。

## task-03：macOS 异步脚本执行器

- [x] task-03: macOS 异步脚本执行器
- 追踪需求：R-02、R-07。
- 依赖任务：task-02。
- 修改范围：desktop/src/adv_helper/os_adapters/script_runner.py 及执行器测试。
- 公共能力处理：新建 OS 适配器；现有 Codex provider 是专用 RPC，不能承担脚本子进程执行。
- 代码注释要求：非交互环境、进程组清理、取消不等于回滚外部效果。
- 完成标准：按设计用 /bin/sh 执行配置原文，以配置目录为 cwd；提供退出码、启动失败、超时和取消清理，事件循环保持响应。
- 自动化验证：无害子进程验证 cwd、非零退出、超时、取消、子进程组终止；假进程验证 TERM/KILL 边界，绝不在自动测试中打开浏览器。
- 人工验证关注点：后续人工触发 Google 验证系统浏览器接受请求；不以退出码证明网页可达。
- 待确认问题：无。

task-03 验证记录：5 项执行器自动化用例完成，含真实无害子进程 cwd/退出码、TERM 抵抗进程组超时、取消及创建窗口取消回收。真实浏览器行为未验收。

## task-04：桌面执行路由与会话集成

- [x] task-04: 桌面执行路由与会话集成
- 追踪需求：R-02、R-04、R-07。
- 依赖任务：task-01、task-02、task-03。
- 修改范围：script_module.py、bootstrap.py、配置路径传递和桌面集成测试。
- 公共能力处理：复用 ActionRegistry、ModuleManager、会话 execId 与发送锁；按 ID/字符复用同一个脚本执行函数。
- 代码注释要求：首次 await 前占忙碌槽、未匹配不执行、清理结束才释放槽。
- 完成标准：注册三个固定能力；ID 执行和全量首键匹配共用忙碌槽；未加载任何列表时也可快捷执行；禁用/非法动作不可执行。
- 自动化验证：假 transport/provider/runner 验证超过 100 条时 hello 大小稳定、末页动作按 ID 可执行、跨页重复键只执行首项、BUSY、未绑定、非法 payload、会话取消；Codex/授时并发不被脚本阻塞。
- 人工验证关注点：最终检查从四栏 Alt+G 的实际效果与结果名称一致。
- 待确认问题：无。

task-04 验证记录：桌面脚本/已有会话、配置和执行器共 54 项自动化断言完成，涵盖全量匹配、末项 ID 执行、忙碌并发和取消清理；真实 BLE 未验收。

## task-05：固件物理按键与优先级路由

- [x] task-05: 固件物理按键与优先级路由
- 追踪需求：R-03、R-04、R-05、R-06。
- 依赖任务：无。
- 修改范围：keyboard_adapter.*、新增纯输入快照/边沿逻辑和 input_router.*、navigation_service 及测试。
- 公共能力处理：扩展现有事件与导航；新建纯输入逻辑以脱离硬件验证，业务执行留待主循环集成。
- 代码注释要求：物理 Fn+Enter、isChange 的限制、主键边沿、Fn 页内特例及多键歧义。
- 完成标准：四栏导航、Codex 专属 Fn+Enter、Alt 字符动作和模块方向映射形成明确路由结果；单独 g 不产生执行动作。
- 自动化验证：native 快照序列测试同键数替换、长按、释放、修饰键前后变化、多主键；遍历四模块的 Fn/Alt/Enter/标点映射组合与关闭映射，验证消费优先级。
- 人工验证关注点：真机按 Fn+1～4、Fn+Enter、Alt+G 和标点方向；确认其他页 Fn+Enter 无效果、长按不重复。
- 待确认问题：无。

task-05 验证记录（2026-09-06）：新增快照边沿和路由矩阵 2 个 native 用例，遍历四模块及 32 种修饰键组合；连同现有导航共 6 个用例完成。覆盖同键数替换、长按、修饰键变化、多主键、Fn+Enter 与独立方向映射。物理库接口已做 ADV 目标编译；实际物理按键待 task-08 接入后验收。

## task-06：ADV 每模块映射配置与串口分发

- [x] task-06: ADV 每模块映射配置与串口分发
- 追踪需求：R-05。
- 依赖任务：task-05。
- 修改范围：新增 input_config 服务、firmware/data/config/input.json；CodexConfigCommands、单一串口分发器及对应测试。
- 公共能力处理：复用 ConfigFileStore；提取现有单一行读取能力，避免两个服务抢读串口。
- 代码注释要求：保存替换语义、失败保留、缺省值、同值不写与串口读预算。
- 完成标准：支持 input.config.read/save/reload，按模块立即应用并持久化；保留 codex.config.* 和 t 诊断入口。
- 自动化验证：假存储/临时目录覆盖缺省和非法值、保存失败/重读失败、独立开关、同值不写；串口半行/超长行/混合前缀回归；Codex 周期保存行为不变。
- 人工验证关注点：串口仅关闭 scripts 映射，检查 Codex 仍可滚动；恢复后重启验证持久化；检查无文件系统时提示且不格式化。
- 待确认问题：无。

task-06 验证记录（2026-09-06）：输入配置及混合串口 2 个 native 用例，连同既有 Codex 配置、串口与文件存储共 8 个用例完成。覆盖默认值、非法字段/类型、替换语义、同值不写、保存或重读失败保留、重新实例化加载、半行、64 字节读取预算与超长行恢复。共用配置 JSON 读取器从 Codex 解析器提取；CodexConfigCommands 暂保留转发到共用 dispatcher 的 poll 兼容入口。启动加载、输入命令和路由器的 main 接线归 task-08，本轮串口组合入口由 native harness 验证。

## task-07：固件分页与脚本请求控制器

- [x] task-07: 固件分页与脚本请求控制器
- 追踪需求：R-01、R-02、R-04、R-07。
- 依赖任务：task-01。
- 修改范围：新增 application/scripts 控制器/状态及 native 测试。
- 公共能力处理：复用 MessageRouter、OutgoingJsonlQueue、ExecIdGenerator 和会话机制；只持有当前页，不建设完整目录缓存。
- 代码注释要求：跨页待加载状态、键执行不依赖目录、超时结果未知、会话隔离。
- 完成标准：第一页/前后页读取与当前页缓存；ID/字符执行共用单在途槽；目录 15 秒、执行 45 秒等待；失败重试只适用于显式目录重读。
- 自动化验证：假时钟/发送队列验证 101 条逐页访问且缓存最多 8 条、首尾/失败页重试/切页、加载中 Enter 不执行旧项、无目录时 Alt 可发、BUSY、超时不重放、迟到/身份变化/断线清理。
- 人工验证关注点：最终从多页列表选中首尾及重复键后项，Enter 执行正确动作；断线后不会自动重复执行。
- 待确认问题：无。

task-07 验证记录（2026-09-06）：新增 8 个 native 用例，配合真实 codec、MessageRouter 与 OutgoingJsonlQueue 验证 101 条前后分页、缓存最多 8 条、空页/首尾、重复键后项按 ID 执行、失败页保持旧缓存且 Enter 仅重读、目录独立于 Alt、共享忙碌槽、能力兼容、队列拒绝、15/45 秒超时与计时回绕、迟到响应和会话失效。控制器提供页内逻辑选中位置供页面读取，页面滚动/绘制留待 task-08。目录条目字段校验复用 task-01 codec，控制器追加请求页与会话 total 一致性校验；未接入真实 BLE/UI。

## task-08：脚本页面、全局反馈与主循环集成

- [x] task-08: 脚本页面、全局反馈与主循环集成
- 追踪需求：R-01～R-07。
- 依赖任务：task-04、task-05、task-06、task-07。
- 修改范围：新增 scripts_page.*；main.cpp、AppShell 与相关集成测试。
- 公共能力处理：复用显示适配、四栏导航和后台任务；增加最小状态条，不建通用列表库。
- 代码注释要求：输入处理顺序、页面切换不取消执行、状态条到期重绘及 UTF-8 裁剪。
- 完成标准：Fn+2 显示脚本列表；上下跨页、Enter 执行、四栏 Alt 全局执行；重复键仅有效首项显示快捷提示；加载/空/失败/执行反馈齐全；Codex 与授时保持工作。
- 自动化验证：native 主循环/控制器协作或等价 harness 覆盖 hello 后请求顺序、目录失败仍快捷执行、模块切换、忙碌反馈、反馈到期刷新；执行相关桌面测试、固件 native 测试和目标构建作基础检查。
- 人工验证关注点：设备逐页验证中文、选中高亮、页边界加载/失败提示；四页 Alt+G 和普通 g；Codex Enter/Fn+Enter；执行中切栏、断线、重连。
- 待确认问题：无。

task-08 验证记录（2026-09-06）：完成四行脚本页、UTF-8 按宽度裁剪、全局状态条及主循环输入/hello/响应/超时/串口接线。新建 `DisplayAdapter::fitText` 供脚本名称和全局反馈共用：原 Codex 标题裁剪为私有估算，不能直接用于这两个不同宽度区域。移除 CodexConfigCommands 临时 poll 兼容入口，只保留单一 ConfigCommandDispatcher。新增页面 3 个、多模块协作 harness 5 个 native 用例；全量 native 84 个、桌面 185 个用例成功，ADV 目标构建 SUCCESS。实际中文像素、键盘扫描、浏览器及 BLE 待 [acceptance.md](acceptance.md) 人工确认。

## task-09：操作文档与人工验收记录

- [ ] task-09: 操作文档与人工验收记录
- 追踪需求：R-01～R-07。
- 依赖任务：task-08。
- 修改范围：README.md、desktop/README.md、firmware/README.md、本规格验收记录。
- 公共能力处理：复用简洁根 README 和详细分层文档，协议细节沿用 task-01。
- 代码注释要求：不涉及生产代码注释。
- 完成标准：记录配置样例、无总条数上限/分页语义、快捷键首项匹配、串口映射配置、执行失败及结果未知语义；保存人工验收步骤和真实结果。
- 自动化验证：静态核对默认 JSON、命令和协议字段与实现一致；引用已执行自动化结果，不为文档改动重复全量构建。
- 人工验证关注点：使用 17 条以上无害脚本配置：跨页逐项浏览，末页 Enter；全局键指向未加载页，跨页重复键执行首项；默认 Google 真机触发；配置保存后重启；中文显示、BLE 重连、Codex/授时回归。未得到用户真实设备结果前标记待人工验收。
- 待确认问题：无。

task-09 当前进度（2026-09-06）：根 README、desktop/README、firmware/README 已更新；新增 [acceptance.md](acceptance.md) 和 [17 条脚本验收配置](manual-config.json)，记录分页、首键匹配、串口保存、结果未确认及可复现的忙碌/失败/超时步骤。配置静态核对无条目错误且页数为 3，未执行其中脚本。人工记录全部明确标为未执行；尚无用户实测结果，故 task-09 保持未勾选。

## 前轮交付验证：task-01～04（2026-09-06）

- 完成范围：task-01～04。task-05 的输入实现及测试已撤回，键盘和导航源码恢复本轮开始前内容；task-05～09 保持未完成。
- `cd desktop && uv run python -m pytest`：185 项自动化断言成功，涵盖原有功能、分页、配置、脚本执行和会话并发。
- `pio test -d firmware -e native -f test_protocol -f test_navigation`：14 项自动化断言成功，覆盖新增协议及恢复后的原导航行为。
- `git diff --check` 未发现空白错误；静态检索未发现 task-05 输入路由或边沿识别的残留引用。
- 默认 Google 配置已增加，但尚未接入设备脚本页和 Alt 按键；本轮未打开浏览器、未刷写、未做真实 BLE/设备验收。设备交互需后续任务完成后验证。

## 阶段确认

任务拆解已确认。task-01～08 完成代码与自动化记录；task-09 操作文档和验收配置完成，待真实设备验收结果。


## 前轮交付验证：task-05～07（2026-09-06）

- `pio test -d firmware -e native`：19 个测试套件、76 个自动化用例成功；包含新增 12 个用例与既有 Codex、授时、配置、导航、协议、队列回归。
- 收尾补充“回包在主循环 tick 之前处理也必须遵守超时”的边界保护；对 `test_scripts_controller` 和 `test_input_router` 共 10 个用例再次执行，并增量编译最终源码。
- `pio run -d firmware -e m5stack-cardputer-adv`：目标编译完成，检查了真实键盘库物理读取接口。构建使用现有依赖与工具链，未修改依赖配置或锁文件。
- `git diff --check` 未发现空白错误。本轮未修改桌面端、共享协议或 main.cpp；前轮 task-01～04 工作保留。
- 本轮仅完成可独立验证的模块能力。设备暂未接入 InputRouter、InputConfigService、ScriptsController 或脚本页面；`input.config.*` 尚不能从当前设备串口调用。task-08 应实例化两个配置命令处理器并只 poll 一个 ConfigCommandDispatcher，启动 reload 输入配置，然后统一接入路由、会话、消息与反馈。
- 未刷写设备、未打开 Google、未进行真实 BLE、中文屏幕、物理键盘或重启持久化验收。这些人工结果继续保持待验收。


## 本轮交付验证：task-08～09（2026-09-06）

- `pio test -d firmware -e native`：21 套件，84 个用例成功。新增场景覆盖四页快捷键、普通字母/未知 Fn 消费、首次请求顺序、跨页反馈、3 秒消退重绘、选择/滚动/中文裁剪、配置及会话集成；物理键盘和 BLE 由 harness 替代。
- `cd desktop` 后 `uv run --locked python -m pytest`：185 个用例成功；未打开 Google。
- `pio run -d firmware -e m5stack-cardputer-adv`：SUCCESS；RAM 51172 bytes，Flash 1563949 bytes。未升级依赖、未刷写或部署文件系统。
- 验收 JSON 通过实际配置加载器静态加载；17 个脚本按 8/8/1 分页，01 为 g 的首项，09 保留重复 g 但无 effectiveKey，17 绑定 z。未调用真实执行器。
- 操作文档与当前代码逐项核对，`git diff --check` 未发现空白错误。真机验收按 [acceptance.md](acceptance.md) 保留待执行状态。

## 完成度复核（2026-09-06）

见 [review.md](review.md)。Task 01～08 代码与自动化已完成；Task 09 文档已交付，真机结果仍待执行。审查修正请求 ID 发送期间占用、Unicode 配置隔离、NUL 元数据校验，并清理重复会话失效处理及占位代码。最新桌面 188 项、native 85 项自动化成功，ADV 目标构建完成。
