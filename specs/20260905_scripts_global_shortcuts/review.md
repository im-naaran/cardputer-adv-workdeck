> 状态：审查及修正完成；真机验收待执行

# 本次任务完成度与代码审查（2026-09-06）

范围为本规格对应的工作区实现，包含未跟踪的新增源码、测试与文档；不是仅比较两个 commit。按需求和 task-01～09 核对，未修改依赖、锁文件或刷写设备。

## 完成情况

| Task | 核对范围 | 结论 |
| --- | --- | --- |
| 01 | 三项固定能力、分页/执行 codec、共享 fixtures、字段及整帧限制 | 代码完成；本次补上含 NUL 字符串的严格校验 |
| 02 | 默认 Google、脚本校验、有效项顺序、8 条分页、重复键首项提示 | 代码完成；本次修正非法 Unicode 导致整份加载失败 |
| 03 | 异步 shell、cwd、非零退出、超时/取消与进程组回收 | 代码完成；保留必要清理逻辑，不作形式上的压缩 |
| 04 | 能力注册、按 ID/字符执行、共享忙碌槽、会话请求关联 | 代码完成；本次延长请求 ID 占用至响应发送结束 |
| 05 | 物理快照、按下边沿、Fn/Alt 优先级、四模块路由 | 代码完成；物理扫描需真机验证 |
| 06 | 独立方向配置、保存重读应用、默认文件、单一串口分发 | 代码完成并接入 main；持久化需真机验证 |
| 07 | 当前页缓存、前后页、显式重读、共享执行槽、超时/会话清理 | 代码完成；不重复实现 codec 的条目字段校验 |
| 08 | 四行列表、选中/裁剪、状态条、输入/hello/回包/tick 接线 | 代码完成；本次展示具体目录失败原因、合并重复清理 |
| 09 | README、17 条验收配置、操作步骤与实际结果 | 文档完成；真实结果仍未执行，任务不能勾选完成 |

因此当前是 **Task 01～08 代码完成，Task 09 待真机验收**，不是全部任务已经验收。

## 本次发现并修正

| 级别 | 问题与影响 | 修正及回归 |
| --- | --- | --- |
| P1 | desktop/bootstrap 在等待发送之前释放 execId。脚本已完成但发送阻塞时，相同 ID 请求可再次执行副作用 | 请求占用延续至发送结束，finally 仅由原请求释放。新增用例在旧代码复现执行两次，修正后只执行一次并返回 BUSY |
| P2 | script 名称或正文含 JSON 可表达的孤立 Unicode surrogate 时，UTF-8 长度计算抛异常，绕过 ConfigError 条目隔离 | 共用 utf8_fits 安全检查；非法条目跳过、有效条目保留。名称和正文两个旧代码失败用例已修正 |
| P2 | 固件读取字符串使用 C 字符串截断，带 NUL 的 actionId/name/key 被当成合法前缀接受 | 使用 JsonString 的实际字节长度，随后由现有字段校验拒绝；新增协议用例先复现再修正 |
| P3 | directoryError 只写不读，页面丢失“未发送/超时”等原因 | 页面使用简短错误原因，Enter 重试提示只在页面维护；补充显示数据断言 |

## 简洁性处理与保留理由

- 三处模块会话失效处理合并为 main 的 `clearModuleSession()`，避免后续遗漏某个模块。
- 删除脚本占位页旧分支，移除 CodexConfigCommands 对串口分发器的无用依赖；串口始终只有一个 poll。
- 保留请求入口、页面、存储、执行器分层；没有增加新的框架、队列或周期任务。
- codec 检查字段和传输结构，控制器检查目标页、同会话 total 和请求关联，属于不同边界；不为减少行数合并。
- native 集成 harness 与 main 存在装配逻辑对应，用于替代硬件；它并非直接运行 Arduino main，因此目标编译与真机验收仍分别保留。
- 已清理本次发现的明显冗余；不作“绝对无冗余/无缺陷”的保证。

## 验证及剩余边界

- 桌面全量：`uv run --locked python -m pytest`，188 项成功（本次新增 3 项）。
- 固件全量：`pio test -d firmware -e native`，85 项成功（本次新增 1 项，另扩展目录错误显示断言）。
- ADV 目标构建：SUCCESS；会话清理合并后补做增量构建。
- `git diff --check` 无空白错误；前轮实现和暂存内容保留，未执行 commit。
- [人工验收](acceptance.md) 的中文显示、物理组合键、真实 BLE、浏览器和重启持久化仍未执行。
- 另有既有会话层边界：desktop 的发送锁只串行发送，未限制等待发送的 request_tasks 数量，也没有独立发送超时；在传输持续阻塞时可能积累任务。本轮修复同 ID 重复执行，未扩展为通用背压重构，不据此宣称所有压力场景已覆盖。
