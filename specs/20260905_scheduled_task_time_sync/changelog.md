> 状态：草稿

# 变更记录

## 2026-09-05

- 完成 Phase 0 当前源码与输入方案检查，生成项目上下文及 Phase 1 需求草稿，等待需求确认。
- 在需求中补齐成功授时基准、重连语义、请求超时、非阻塞分片发送和现有分钟级重绘的统一调度边界；未修改生产代码。
- 用户回复“继续”，确认 Phase 1 需求规格，requirements 状态更新为已确认。
- 完成 Phase 2 技术设计草稿：明确调度接口、4 槽发送队列、共享请求 ID、Codex 210 秒总等待、授时 15 秒等待及 2 次短时重试、成功时间基准和平台范围校验；等待设计确认。
- 用户回复“继续”，确认 Phase 2 技术设计，design 状态更新为已确认。
- 完成 Phase 3 任务草稿，共 15 项，包含公共能力、双端协议、控制器、页面、编排、自动化回归和独立真机验收；等待任务确认，未修改生产代码。
- 用户回复“继续”，确认 Phase 3 任务清单，进入 Phase 4 实现。
- 用户追加“先做到 task08”，本轮范围收缩为 task-01～08；已开始的后续实现撤回，原 Codex 周期/页面保留，task-09～15 不执行。
- 完成 task-01：固定容量调度器与生命周期接口，复用 elapsedMs。
- 完成 task-02：回调快照/代次、自取消、重入保护与回绕测试；调度组最终 4 项无失败。
- 完成 task-03：开机级共享 64 位 ExecIdGenerator，计数耗尽不复用；1 项测试无失败。
- 完成 task-04：4 槽 JSONL 发送队列，20 bytes/10 ms 单片发送、过期和取消；2 项测试无失败。
- 完成 task-05：BLE 入队及主循环单片发送接入、连接代次和旧消息清理；保留现有控制器的 sendJsonl 过渡适配；2 项 transport 测试及既有 JSONL 用例无失败。
- 完成 task-06：双端 system.time.read 协议、严格类型校验及共享成功/非法 fixtures；固件协议组 7 项无失败。
- 完成 task-07：桌面独立默认授时 handler，不受 Codex 禁用/慢查询影响；相关单元/集成验证纳入桌面全量 88 项，均无失败。
- 完成 task-08：SystemTimeService 与可替换平台时钟，UTC/偏移/time_t 边界和失败原子提交；2 项测试无失败。
- 验证记录：固件全量 native 38 项无失败，追加测试后定向 6 项无失败（当前 39 个用例）；桌面 `uv run python -m pytest` 88 项无失败；设备构建成功，Flash 1482653 bytes / RAM 49820 bytes；未刷写、未做真机时间精度或真实 BLE 验收。
- 环境记录：pytest 脚本仍指向旧项目，采用 Python 模块入口运行；uv sync --locked 未改锁文件。实际固件构建解析 framework 3.20016.0，平台时间范围以编译类型为准。
- 用户指定继续 task-09、10、11、12，本轮执行范围扩展到 task-12，task-13～15 不执行。
- 完成 task-09：Codex 迁移统一调度和共享 execId，切页仍刷新、手动提交成功才重设起点、210 秒超时释放，错误不改变成功缓存年龄；删除旧 RefreshPolicy 两个文件，原内容保留在用户暂存区。Codex 6 项定向用例无失败。
- 完成 task-10：新增授时 Controller、首次及成功后一小时同步、15 秒超时、响应关联和成功年龄饱和记录；公共服务复用既有实现。设备 main 接线仍属于 task-13。
- 完成 task-11：授时失败 3 秒后最多 2 次重试，入队拒绝计次数，耗尽后等周期/重连；同电脑新鲜重连不推迟截止时间，换电脑/到期重连补同步，暂停和长断线回绕有覆盖。授时 9 项定向用例无失败。
- 完成 task-12：Fn+Enter 自动开关、即时置脏、实际周期标签和所有已连接页面状态的自动状态页脚。页面 7 项、导航 4 项无失败；UI 真机结果待确认。
- 本轮验证：上述用例加系统时间 2 项，共 28 个不同 native 用例无失败；设备构建完成，Flash 1486901 bytes / RAM 50228 bytes，git diff --check 无报错。未刷写或执行真机测试；停止在 task-12。

- 用户要求继续剩余任务，本轮范围扩展到 task-13～15，不重复阶段审批。
- 完成 task-13：接入 TimeSyncController、共享系统时间、hello/授时响应路由，身份切换和非法 hello 清空双动作；统一注册授时/Codex/分钟显示任务，移除临时发送适配和独立分钟计时。
- 完成 task-14：新增会话与双任务集成共 6 项用例；最终固件全量 native 54 项无失败，桌面全量 88 项无失败。首次回归发现旧 JSONL 测试仍用 sendJsonl，迁移到 enqueueRequest 后全量重跑。
- task-15 文档部分完成：更新双端 README 和协议运行语义，增加串口 t 按需时间诊断和一小时 E0/E1 测量说明；未刷写、未执行真机测量，任务保持未勾选。
- 最终设备构建完成，Flash 1490249 bytes / RAM 50380 bytes；git diff --check 无报错。额外的全仓库 cached 空白检查报告原有文档/文件空白问题，未清理无关内容；本轮未执行暂存、提交或刷写。

- 提交前文档整理（2026-09-05）：用户反馈已按本地步骤完成 ADV 等检查；本地准备材料及独立验收清单不提交，正式文档改为引用 specs 中的验收步骤，反馈记录在 tasks.md，不补录未提供的测量数值。
