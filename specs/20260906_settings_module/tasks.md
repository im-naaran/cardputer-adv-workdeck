# 设置模块任务拆解

> 状态：已确认
> 日期：2026-09-06
> 阶段：Phase 4，已授权执行至 task-11；task-10 已完成，task-11 待真机反馈

依据本目录已确认的 requirements.md（R-01～R-08）及 design.md。task-01～task-09 已完成代码及相应自动化检查，task-10 文档已完成；task-11 已准备验收表，尚无真机结果。

## 任务清单

- [x] task-01: [配置] 亮度与 Wi-Fi 配置校验及持久化服务
- [x] task-02: [输入] 实际文本字符与设置编辑路由
- [x] task-03: [Wi-Fi] 公共连接服务、使用权及关闭状态机
- [x] task-04: [Wi-Fi] 扫描与草稿测试的临时需求
- [x] task-05: [平台] ADV Wi-Fi 适配与亮度控制
- [x] task-06: [设置] 亮度与 Codex 周期控制和页面
- [x] task-07: [设置] Wi-Fi 明文表单、扫描列表和结果展示
- [x] task-08: [集成] 开机加载、离线输入与主循环接线
- [x] task-09: [回归] 公共服务调用验证及固件集成检查
- [x] task-10: [文档] 设备操作、公共接口及人工验收记录
- [ ] task-11: [真机] 设置持久化、无线静默及 BLE 共存验收

## 执行与记录约定

- 按编号串行推进；每项只修改标注范围及对应测试。每项完成更新本清单与 changelog，记录实际命令、结果和未覆盖项。
- 本期不升级依赖、不改锁文件或分区布局，不修改 focus-clock，不新增桌面协议和实际联网业务。复用现有原生测试框架、配置存储及 Codex 服务。
- 文件路径均相对项目根目录，测试命令使用 `pio test -d firmware -e native -f <suite>`；可使用本机已安装的 PlatformIO 可执行文件。
- 单项先运行对应逻辑测试；task-05 检查平台接口构建，task-09 统一跑完整固件 native 回归和最终构建。除新修改、失败或未解决风险外不重复扩大验证。
- 不自动刷写、上传文件系统或擦除 Flash。真机任务单独记录；自动化结果、构建结果不能替代实际亮度、中文像素、无线连接及断电验收。
- 尚未接入主循环的服务可独立测试和撤回；已经被后续任务引用时，回滚按依赖逆序进行，避免留下不可编译调用。

## task-01: [配置] 亮度与 Wi-Fi 配置校验及持久化服务

执行记录（2026-09-06）：新增 DisplayConfigService、WifiConfigService，提供严格编解码、read/reload/save、保存快照与配置状态；未接入屏幕或无线。test_display_config、test_wifi_config 共 4 个测试用例无失败；既有 test_config_file_store 1 个用例无失败。文件系统实际断电行为待 task-11。

- 追踪需求：R-02、R-03、R-04、R-07、R-08。
- 依赖任务：无。
- 修改范围：`firmware/src/application/settings/display_config.*`、`application/wifi/wifi_config.*`（同属 firmware/src）；`firmware/test/test_display_config/`、`test_wifi_config/`。
- 公共能力处理：复用 ConfigFileStore、ConfigStatus、ConfigInput 和 ArduinoJson；已有存储满足 512 字节上限，新增模块配置服务，不增加总配置框架或 Wi-Fi type 字段。
- 代码注释要求：说明按字节校验、企业/普通密码规则差异、保存和应用分离、同值跳过写入但允许失败重试。
- 完成标准：亮度默认第 3 档且只接受 1～5；Wi-Fi 三字段严格解析，用户名决定认证；实现 read/reload/save 和有效保存快照；保存不依赖无线、不启用连接。缺失、损坏、写失败和回读失败分别反馈，失败不把草稿冒充已保存值。
- 自动化验证：新套件覆盖全部字段边界、非法 UTF-8/控制字符、JSON 尾随垃圾、引号反斜杠最大转义、空用户名、企业短密码、开放网络、同值保存、写/回读失败及模块隔离；复用既有存储故障注入能力。
- 人工验证关注点：无页面接线；设备实际文件保存与重启恢复留 task-11。
- 待确认问题：无。

## task-02: [输入] 实际文本字符与设置编辑路由

执行记录（2026-09-06）：KeyEvent 追加 text，InputSnapshot 追加 Shift 映射；键盘适配器归一化 Tab/Backspace；InputRouter 增加默认关闭的 textEditing 参数。既有调用不进入编辑模式。test_input_router 3 个、test_input_config 2 个、test_navigation 4 个、test_scripts_integration 5 个用例无失败。尚未进行 Arduino 目标构建或物理键盘验收，页面编辑和中文退格属于后续页面任务。

- 追踪需求：R-01、R-03。
- 依赖任务：无。
- 修改范围：`firmware/src/platform/keyboard_adapter.*`、`core/input_router.*`；`firmware/test/test_input_router/` 及必要键盘跟踪测试。
- 公共能力处理：扩展现有 KeyEvent、KeyPressTracker 和 InputRouter，保留原调用默认行为，不另建全局输入系统。
- 代码注释要求：解释物理键身份与 Shift 后字符分离、编辑上下文优先于方向映射、Fn/Alt 既有优先级。
- 完成标准：生成真实大小写/符号、Tab/Backspace 事件；编辑时 `; , . /` 可输入，数字 1～4 不导航；非编辑页面保持原行为，按下沿去重不退化。
- 自动化验证：运行 test_input_router、test_input_config、test_navigation；覆盖 Shift 数字/字母、Fn+数字/方向、Alt 字母、修饰键混合、释放后再次按下及方向映射关闭。
- 人工验证关注点：task-11 从设置字段实测特殊字符；自动化仅证明事件和路由逻辑。
- 待确认问题：无。

## task-03: [Wi-Fi] 公共连接服务、使用权及关闭状态机

执行记录（2026-09-06）：新增 WifiService 与 WifiAdapter 接口，提供配置检查、按保存值连接、requestId 状态查询、实际连接判断、close 和 tick。模块成功后保持，失败/超时/断线释放；过期关闭不影响新请求，清理失败保留使用权并允许重试。状态分为 phase 与 outcome，关闭后仍保留失败原因。test_wifi_service 5 个用例无失败；未实现 ESP32 适配或接入主循环。

- 追踪需求：R-04、R-07、R-08。
- 依赖任务：task-01。
- 修改范围：`firmware/src/application/wifi/wifi_service.*`、`platform/wifi_adapter.h`（接口）；`firmware/test/test_wifi_service/` 及其 fake 适配器。
- 公共能力处理：新建独立 WifiService，复用配置服务及单调时钟；未发现已有 Wi-Fi 服务。一个实例负责使用权，不引入引用计数或多消费者框架。
- 代码注释要求：解释 canConnect 不保证真实连通、请求身份防误关、连接快照、模块成功保持连接与失败自动释放。
- 完成标准：提供 configurationStatus/canConnect/connect/status/isConnected/close/tick；默认静默、连接前重读配置、Busy 互斥、30 秒超时、模块成功保持至 close。旧 requestId 不影响新请求，关闭失败可用同一身份重试且不虚报关闭。结果与清理阶段分别保留，失败原因不能被 Closed 覆盖。
- 自动化验证：fake 覆盖配置缺失/损坏/读取失败不启用无线、成功取得 IP、旧 IP 不误判、重复/过期关闭、连接中取消、标识耗尽、使用中断线、关闭失败、保存新配置不抢占、下一次读取新值、毫秒回绕。检查无请求 tick 不调用连接及开关接口。
- 人工验证关注点：本任务验证公共契约；真实联网和关闭无线留 task-11。
- 待确认问题：无。

## task-04: [Wi-Fi] 扫描与草稿测试的临时需求

执行记录（2026-09-06）：同一服务新增 scan/test，使用固定 32 项扫描结果，按 SSID 去重及信号排序；扫描/测试完成自动释放并保留结果，草稿测试不读写配置。test_wifi_scan 3 个用例无失败，test_wifi_config 回归 2 个用例无失败。本轮共 10 个用例无失败，实际扫描、无线关闭与认证留后续平台实现及真机验收。

- 追踪需求：R-03、R-05、R-08。
- 依赖任务：task-03。
- 修改范围：`firmware/src/application/wifi/wifi_service.*`、`platform/wifi_adapter.h`；`firmware/test/test_wifi_service/`、`test_wifi_scan/`。
- 公共能力处理：扩展同一服务的 scan/test 需求，复用 task-03 连接与释放逻辑，参考 focus-clock 异步扫描与测试清理，不复制连接状态机。
- 代码注释要求：说明测试草稿不保存、成功自动释放与普通 connect 的差别、结果复制后释放驱动内存。
- 完成标准：扫描 15 秒、测试 30 秒截止；扫描不连接；SSID 去重取强、最多 32 项并反馈截断。扫描/测试所有终态关闭无线但保留结果；模块持有连接时返回 Busy，草稿修改不污染在途快照。
- 自动化验证：覆盖无结果/失败/超时、去重排序与容量、隐藏 SSID、成功后关闭、关闭失败重试、测试不写配置、模块占用/重复触发、结果快照和过期 requestId。
- 人工验证关注点：扫描列表内容及企业测试留 task-11；不以 fake 成功证明真实认证。
- 待确认问题：无。

## task-05: [平台] ADV Wi-Fi 适配与亮度控制

执行记录（2026-09-06）：新增 PlatformWifiAdapter，提供按需 STA、普通/开放/无证书 PEAP、旧认证/IP 清理、异步扫描及停止、Wi-Fi 关闭；扩展 DisplayAdapter::setBrightness(uint8_t)。实际解析框架为 Arduino ESP32 2.0.16，已核对对应头文件；扫描停止使用库 protected 扩展点复位运行标记和计时，避免下一轮停留在旧扫描。目标构建最终完成：RAM 51,196 / 327,680 字节，Flash 1,569,993 / 3,342,336 字节。test_wifi_service、test_wifi_scan、test_display_config 共 10 个用例无失败。适配器未接入 main，链接裁剪后的大小不代表后续集成资源用量；实际亮度与无线行为未验收。

补充人工关注：错误密码首次连接及超时后静置，确认库内部首连重试不会在释放后重新启用无线；连续扫描及超时后再次扫描，确认异步驱动事件不污染下一轮。

- 追踪需求：R-02、R-03、R-05、R-08。
- 依赖任务：task-04。
- 修改范围：`firmware/src/platform/wifi_adapter.*`、`display_adapter.*`；必要的平台调用验证用例。
- 公共能力处理：实现既定 WifiAdapter，扩展 DisplayAdapter；核对已安装 M5Cardputer/M5GFX/Arduino ESP32 接口，参考 focus-clock PEAP 调用，不升级依赖。
- 代码注释要求：说明无需求 WIFI_OFF、禁用驱动持久化和自动重连、企业认证状态清理顺序、仅关闭 Wi-Fi 而不影响 BLE。
- 完成标准：提供异步扫描/查询/停止、普通/开放/PEAP 连接、实际连接快照、断开清理及关闭；账号同时用于 identity 和 username，证书为空。亮度支持 51/102/153/204/255；不使用长 delay 或 waitForConnectResult。
- 自动化验证：`pio run -d firmware` 检查实际平台编译/链接；运行 test_wifi_service 保证接口适配未破坏契约。检查实现的终止路径确实调用停止/关闭 API。构建不证明底层无线行为，原生 fake 不覆盖 Arduino 驱动。
- 人工验证关注点：task-11 检查五档可见、企业转普通、扫描停止和 WIFI_OFF 后 BLE 正常。
- 待确认问题：无。

## task-06: [设置] 亮度与 Codex 周期控制和页面

执行记录（2026-09-06）：新增 SettingsController / SettingsPage，提供首页、五档亮度、整数分钟编辑与显式保存。亮度先应用后保存，写失败保留当前档并可 Enter 重试；Codex 复用原服务，90 秒原值进入页面不取整。设置三套用例最终共 11 个无失败；Codex 配置/刷新 10 个回归用例无失败。像素、亮度和断电恢复尚未验收。

补充人工关注：Fn+4 → 屏幕亮度，左右或 Tab 调档，Enter 重试；Codex 入口 Enter 编辑、Enter 完成编辑，再选择“保存”，非法值保留输入；“查看保存及运行状态”显示文件值及当前运行值。

- 追踪需求：R-01、R-02、R-06、R-07。
- 依赖任务：task-01、task-02、task-05。
- 修改范围：`firmware/src/application/settings/settings_controller.*`、`settings_page.*`；`firmware/test/test_settings_controller/`、`test_settings_page/`。
- 公共能力处理：复用 AppShell、DisplayAdapter 和 CodexConfigService；设置页内部建立最小焦点/编辑状态，不改 Codex 任务调度或另存周期。
- 代码注释要求：说明亮度先应用再保存及失败重试、分钟换算、非整分钟旧值只展示不改写。
- 完成标准：提供首页三个入口及亮度/Codex 子页。亮度即时生效保存；Codex 1～60 整数分钟显式保存。无效输入保留草稿；自动关闭、同值保存、在途查询等语义沿用现有服务。完整字段查看状态通过字段焦点下的查看操作进入，保证可发现。
- 自动化验证：测试控制器保存调用、亮度失败保持及重试、非法/边界分钟、90 秒原值保留；运行 test_codex_config、test_codex_refresh。页面测试检查焦点/返回/越界与状态，不用固定像素断言代替人工检查。
- 人工验证关注点：task-11 从 Fn+4 进入，逐档修改并重启；周期保存后检查运行值；方向映射关闭时用 Tab/Enter/Backspace 操作。
- 待确认问题：无。

## task-07: [设置] Wi-Fi 明文表单、扫描列表和结果展示

执行记录（2026-09-06）：补齐 SSID/用户名/密码明文编辑、UTF-8 退格、字节上限、分页完整查看、扫描选择、保存与草稿测试。扫描选择只改 SSID；结果保留当次快照及历史 IP，标记草稿变化/测试未保存；保存反馈独立保留。Busy 不抢占，关闭失败可按请求身份重试；外部模块释放触发重绘。最终设置控制器 6 个、页面 4 个用例无失败，未做真实无线或上屏验收。

补充人工关注：字段 Enter 编辑、Enter 完成、Tab 完成后移到下一字段；字段右键查看，或 Tab 找到“查看完整SSID/用户名/密码”入口；查看状态 Tab 翻页、Enter 返回。“查看操作及测试结果”保留完整错误与测试记录，“重试关闭Wi-Fi”只重试设置自己的未释放请求。

- 追踪需求：R-03、R-04、R-05、R-07、R-08。
- 依赖任务：task-04、task-06。
- 修改范围：`firmware/src/application/settings/settings_controller.*`、`settings_page.*`；`firmware/test/test_settings_controller/`、`test_settings_page/`。
- 公共能力处理：复用 WifiConfigService、WifiService 和已有绘制；字段编辑为页面内部能力，不新增通用表单库。
- 代码注释要求：说明草稿/保存值/测试快照分离、UTF-8 退格、测试历史不代表在线、扫描选择不覆盖凭据。
- 完成标准：三字段全部明文，无类型选项；支持键盘输入、长值查看、扫描选择、保存、测试、关闭失败重试。保存不启用 Wi-Fi；测试结束关闭后仍显示结果，编辑后旧结果标记过期；Busy 不抢占模块连接。
- 自动化验证：模拟输入大小写/符号/空格、中文 SSID 退格、超限拒绝、首尾空格保留、清空用户名后密码重新校验；扫描选中只改 SSID；保存/测试分别调用；结果释放和切页草稿保留。
- 人工验证关注点：task-11 从 Wi-Fi 表单编辑长 SSID/用户名/密码，分页查看完整值；验证扫描空结果、测试失败、Busy 与关闭失败文案可读。
- 待确认问题：无。

## task-08: [集成] 开机加载、离线输入与主循环接线

执行记录（2026-09-06）：main 注入唯一 WifiService 及设置控制器/页面；挂载后尽早恢复亮度，加载 Wi-Fi 只读不连接。设置输入/绘制置于 BLE 离线拦截前，保留 Fn/Alt 优先级；Wi-Fi 全局 tick、状态变化重绘，操作后重新采样单调时钟。原生接缝直接编译真实 main.cpp 的 setup/handleKey/handleConnection/loop，替换存储、时钟及 Wi-Fi 平台依赖，不复制主循环。真实接线用例 1 个无失败；输入路由/脚本集成回归 8 个无失败。

补充人工关注：离线 Fn+4 编辑，测试中 Fn 切页并断开 BLE，再回设置确认草稿与历史结果；等待脚本反馈消失后检查页脚恢复。已完成目标编译检查；构建资源及命令见 changelog，仍不代表无线已实际关闭或 BLE 共存已验收。

- 追踪需求：R-01～R-08。
- 依赖任务：task-05、task-07。
- 修改范围：`firmware/src/main.cpp`；`firmware/test/test_settings_integration/`，按需要扩展现有测试接缝，不建立第二套主循环。
- 公共能力处理：复用原有依赖注入、AppShell、InputRouter、单调时钟和重绘标记；Wi-Fi 全局唯一实例，继续由原主循环驱动。
- 代码注释要求：说明设置分支在离线拦截前、加载 Wi-Fi 不连接、切页只影响显示、操作耗时后刷新单调时间。
- 完成标准：启动恢复亮度/Codex/Wi-Fi 配置并保持 WIFI_OFF；Fn+4 离线可操作。Wi-Fi tick 不依赖当前页面或 BLE 会话，设置错误不影响其他模块；按状态变化重绘，不每帧读文件。
- 自动化验证：通过集成测试覆盖开机默认/保存值、离线输入分派、切页期间测试结束释放、BLE 断线不丢设置、Fn/Alt 旧行为、查询状态不连接、保存与 Codex 调度互不覆盖。
- 人工验证关注点：task-11 检查离线开机、测试中切页和 BLE 断线；上屏首帧亮度、中文排版仍须实测。
- 待确认问题：无。

## task-09: [回归] 公共服务调用验证及固件集成检查

执行记录（2026-09-06）：新增公共模块完整生命周期和终态静默的 2 个用例，覆盖配置检查、连接中、成功后长时间持有、显式关闭、重复关闭、时间回绕，以及启动失败/连接失败/超时/取消后的释放与下一次请求。扩展真实 main.cpp 接线用例，覆盖模块持有期间切页/BLE 断线、设置 Busy、保存新配置不抢占、关闭后模拟 24 小时无重连/文件轮询、下一次读取最新配置及旧身份隔离。本轮未修改生产代码。

自动化记录：`pio test -d firmware -e native`，28 个套件、111 个用例无失败；`pio run -d firmware` 完成，RAM 74,564 / 327,680 字节（22.8%），Flash 2,089,909 / 3,342,336 字节（62.5%）。`git diff --check` 无空白错误。详细命令及边界见 changelog。

留给 task-11：fake 只证明调用责任与状态/清理调用，不证明真实驱动 WIFI_OFF、企业认证、扫描异步事件、BLE 共存、中文像素、实际亮度或断电持久化；这些项目仍需设备证据及用户确认。

- 追踪需求：R-01～R-08。
- 依赖任务：task-08。
- 修改范围：`firmware/test/test_wifi_service/`、`test_settings_integration/` 及本规格执行记录；仅对发现的本期缺陷做对应范围修复。
- 公共能力处理：用 fake 调用模块复用真实 WifiService，不增加生产联网模块或测试专用业务入口。
- 代码注释要求：说明测试调用者成功后必须关闭，以及 fake 对实际无线的验证局限。
- 完成标准：完整验证配置检查 → connect → Connected → 业务保持 → close → Closed；错误/取消/忙路径不泄漏使用权，已关闭后长期 tick 不再启动无线。所有自动化失败均处理或明确记录阻塞，不能带着未知失败标完成。
- 自动化验证：运行 `pio test -d firmware -e native` 和 `pio run -d firmware`；记录测试数量、失败情况及构建资源输出。真实接线的读取、路由和生命周期应有集成断言，不仅测试独立辅助函数。
- 人工验证关注点：无新增 UI；输出仍未覆盖的底层驱动和设备行为供 task-11 执行。
- 待确认问题：无。

## task-10: [文档] 设备操作、公共接口及人工验收记录

执行记录（2026-09-07）：更新根 README 设置入口与部署边界，固件 README 补齐三项操作、字段约束、保存/测试差异、公共接口及非阻塞调用示例；移除设置占位/未来接入说明。新增 acceptance.md，含 23 项步骤、预期、实际结果及证据栏，未执行结果留空。定向核对源码接口/路径/按键/默认值和文档链接，未为文档变更重新运行构建或测试。

- 追踪需求：R-01～R-08。
- 依赖任务：task-09。
- 修改范围：根 `README.md`、`firmware/README.md`、本规格 `acceptance.md`、任务记录与 changelog。
- 公共能力处理：复用既有文档结构；根 README 只保留入口和关键行为，接口详情放 firmware/README。
- 代码注释要求：不涉及生产代码；示例说明 requestId 生命周期、canConnect 含义和 close 的调用责任。
- 完成标准：移除设置占位说明；记录三项配置操作、AUTO 静默、用户名分流、保存/测试差异、公共服务调用范式及错误处理。说明新文件首次保存创建、既有文件系统前提及 uploadfs 覆盖边界。acceptance.md 提供步骤/预期/实际结果/证据栏，未执行项目留空。
- 自动化验证：定向核对接口名称、文件路径、快捷键和默认值与实际实现一致；无须为文案新增测试。
- 人工验证关注点：检查用户能按文档完成扫描、明文编辑、保存、测试和错误恢复；真实结果在 task-11 填写。
- 待确认问题：无。

## task-11: [真机] 设置持久化、无线静默及 BLE 共存验收

准备记录（2026-09-06）：已建立 acceptance.md 并记录现有 bin 摘要、Task 09 自动化证据及取证限制。只读检查未发现可识别的 ADV USB 串口；尚未收到用户本轮设备/网络实测结果。当前串口未提供 Wi-Fi 状态和故障注入接口，相关项目需专门环境取证。本任务保持未完成，实际结果/证据留空；未刷写、未操作真实网络。

- 追踪需求：R-01～R-08。
- 依赖任务：task-10。
- 修改范围：本规格 `acceptance.md`、任务记录与 changelog；无预设代码变更。
- 公共能力处理：使用实现的页面和服务；不将 focus-clock 的成功记录算作 ADV 验收。
- 代码注释要求：不涉及；记录实际设备、固件版本、步骤、结果及观察方法。
- 完成标准：用户逐项确认下述真机结果后才勾选；未具备环境的项目标为待验收，不以自动化替代。
- 自动化验证：沿用 task-09 结果；有修复时才重跑受影响验证。公共模块生命周期已有 fake 验证，真实驱动开关仍需设备证据。
- 人工验证关注点：离线开机进入 Fn+4；五档亮度与保存失败重试；三项保存断电重启恢复；长字段、中文 SSID 和符号完整输入/查看；普通/公司认证、错误密码、不可用网络、企业转普通；扫描/测试成功、失败、超时后关闭且静置不重连；通过驱动状态或诊断证据确认关闭，不能仅凭页面文案；扫描/测试时 BLE、脚本、Codex 刷新与授时仍正常；文件缺失/损坏及受控写失败不破坏其他配置。
- 待确认问题：无设计阻塞；执行到此需要真实设备、网络与用户验收反馈，未经反馈保持未完成。

## 待确认

本次已授权 task-10、task-11；task-10 完成，task-11 等待设备与用户逐项实测反馈。验收步骤及空白结果栏见 acceptance.md；未将 Task 09 的自动化证据记为真机验收。
