# 设置模块变更记录

> 状态：草稿

## 2026-09-06 — Phase 1 需求确认

- 用户确认无证书企业 Wi-Fi、连接成功判定、亮度档位和 Codex 周期范围。
- requirements.md 标记为已确认，进入 Phase 2；未修改生产代码。
- 用户指定 focus-clock 为 Wi-Fi 认证实现参考；设计核对其 PEAP 调用、企业状态清理和异步扫描，不引入网页配置流程。

## 2026-09-06 — Wi-Fi 改为固定 AUTO 按需启用

- 按用户明确指令取消类型展示及 type 持久化字段，以用户名是否为空选择普通/企业认证。
- 保存与重启只恢复配置，不启用 Wi-Fi；扫描和测试临时启用，成功、失败、超时结束均释放并关闭，不后台重连。
- 测试结果与在线状态分离，关闭无线后仍可查看结果；无实际业务消费者，不建设完整消费者管理框架。
- 同步更新 requirements.md、design.md 和上下文；需求变更来自用户明确指令，技术设计仍为草稿，未修改生产代码。

## 2026-09-06 — 补充公共 Wi-Fi 服务

- 按用户指令新增 R-08，提供配置检查、是否允许尝试连接、实际连接状态、异步连接及关闭能力。
- Wi-Fi 设计独立为 application/wifi/WifiService，设置与后续模块共用；区分测试自动关闭与模块成功后显式关闭。
- 明确单使用者互斥、请求身份校验、失败自动清理和配置更新边界，并补充模拟模块验收。
- 需求同步更新，设计保持草稿；未修改生产代码或进入任务阶段。

## 2026-09-06 — Phase 2 设计确认

- 用户回复“继续”，确认含固定 AUTO 策略及公共 Wi-Fi 服务的当前设计。
- design.md 标记为已确认，生成 Phase 3 tasks.md 草稿；尚未编码。

## 2026-09-06 — 授权执行 task-01、task-02

- 用户明确要求开始 task-01、task-02，进入 Phase 4，仅执行这两项。

## 2026-09-06 — task-01 完成代码与原生测试

- 新增亮度与 Wi-Fi 配置类型、严格 JSON/UTF-8/字节边界校验及 read/reload/save 服务，复用 ConfigFileStore；保存不启用无线，同值跳过写入并支持失败后回读恢复。
- 命令：`pio test -d firmware -e native -f test_display_config -f test_wifi_config`，4 个用例无失败；最后补充头文件和校验循环整理后重跑，同为 4 个无失败。
- 初次运行受 PlatformIO 用户缓存写权限限制，使用获准的扩权执行后完成；未更改缓存权限或依赖。
- 实际文件系统保存、断电恢复及亮度输出未验收。

## 2026-09-06 — task-02 完成代码与原生测试

- 分离物理键身份与实际文本字符，支持 Shift 字符、Tab、Backspace；显式编辑上下文绕过符号方向映射，保留 Fn/Alt 优先级与默认页面行为。
- 命令：`pio test -d firmware -e native -f test_input_router -f test_input_config -f test_navigation -f test_scripts_integration -f test_config_file_store`，15 个用例无失败（包含 task-01 存储回归 1 个）。两组共 19 个用例，无失败。
- `git diff --check` 未发现空白错误；未暂存或提交本轮文件，保留既有暂存内容。
- 未运行目标构建、刷写或真机输入验收；未开始 task-03 及后续任务。

## 2026-09-06 — task-03 公共 Wi-Fi 服务完成

- 用户授权 task-03、task-04；新增 WifiAdapter 抽象接口与 WifiService，未实现硬件适配。
- 配置就绪不代表连通；connect 重读保存配置并取得单一使用权。模块成功保持连接，失败/超时/断线关闭；请求标识校验防止旧请求误关，关闭失败允许持有者重试。
- 状态采用 phase/outcome 分离，关闭阶段不会覆盖最终原因。原生覆盖无配置、忙、保存快照、取消、过期关闭、标识耗尽、时间回绕、无效 IP 与连接身份丢失。

## 2026-09-06 — task-04 扫描与草稿测试完成

- 新增 scan/test 临时需求，复用连接及释放路径；扫描不连接、测试不保存，结束关闭且保留结果。
- 扫描最多 32 项，过滤无效 SSID，去重取强，保留容量截断标记；驱动结果复制后释放。
- 最终命令：`pio test -d firmware -e native -f test_wifi_service -f test_wifi_scan -f test_wifi_config`，10 个用例无失败（5 + 3 + 2）；初轮 9 个用例无失败后补充无效 IP/身份丢失回归并重跑。
- 未运行目标构建、未刷写、未进行真实 Wi-Fi/BLE 验收；task-05 及后续未执行。保留用户既有暂存内容，未暂存或提交本轮改动。

## 2026-09-06 — task-05 平台适配完成

- 用户授权 task-05；新增 PlatformWifiAdapter，实现按需启用、RAM 凭据、企业认证清理、DHCP 旧 IP 清除、异步扫描/停止及关闭 Wi-Fi；提供 DisplayAdapter 亮度接口。
- 修正依赖记录：实际目标构建使用 Arduino ESP32 2.0.16，重新核对该安装目录，未改变依赖或锁文件。
- `pio test -d firmware -e native -f test_wifi_service -f test_wifi_scan -f test_display_config`：10 个用例无失败；这些用例验证服务契约，不覆盖真实驱动。
- `pio run -d firmware` 最终完成。扫描清理增量构建曾因直接访问 protected 接口失败，改为库派生扩展点后重新构建完成。RAM 51,196 字节、Flash 1,569,993 字节；未接入主循环的代码可能被链接器裁剪，不据此承诺最终资源占用。
- 首次认证失败到释放后静默、连续/中断扫描的异步事件及 BLE 共存仍待真机验收；普通连接在无法可靠归属底层失败事件时由服务 30 秒超时结束。
- 未刷写，未开始 task-06 或后续任务，未暂存或提交本轮文件。

## 2026-09-06 — task-06 亮度与 Codex 设置完成

- 用户明确授权继续 task-06、task-07、task-08，按既有已确认需求/设计进入执行；未扩展至 task-09～task-11。
- 新增 SettingsController / SettingsPage：首页三个入口，亮度即时应用及保存失败重试；Codex 整数分钟编辑后显式保存，复用 CodexConfigService 和既有调度，保留 90 秒等原值。
- 设置测试覆盖默认/保存值、亮度写失败及回读失败、非法/边界分钟、自动关闭状态、ApplyFailed、页面焦点/返回/方向映射关闭。
- Codex 配置/刷新回归 10 个用例无失败；设置控制器与页面最终分别为 6 个、4 个用例无失败（其中含 task-07 场景）。
- 人工亮度、中文显示和重启恢复未验收。

## 2026-09-06 — task-07 Wi-Fi 设置交互完成

- 新增明文表单、尾部编辑、UTF-8 退格及完整分页查看；按实际中文字宽折行，超字节上限拒绝新增字符。显式查看入口可用 Tab 到达，不依赖方向映射。
- 扫描选择只替换 SSID；保存与测试分别调用已有服务。保留保存反馈、测试快照和历史 IP，草稿修改或测试未保存时明确标示；Busy 不抢占，关闭失败仅按设置拥有的请求身份重试。
- 保存值、编辑草稿和测试快照分别保留；外部模块释放后触发重绘，终态不每帧复制扫描结果或读取配置文件。
- 设置控制器/页面测试覆盖符号及空格、中文退格、长度拒绝、清空用户名后的密码校验、扫描只改 SSID、保存不启用无线、测试不保存、失败重试、分页查看及草稿保留。
- Wi-Fi 实际扫描、认证、关闭和 BLE 共存未验收。

## 2026-09-06 — task-08 主循环接线完成

- main 注入唯一 Wi-Fi 服务和设置依赖；显示初始化、挂载、恢复亮度后初始化 BLE，随后恢复 Codex/Wi-Fi 配置。启动不申请无线需求。
- 提取单事件 handleKey，仍由原有 handleKeyboard 调用；设置分派及绘制优先于 BLE 离线拦截。Wi-Fi tick 与页面/会话无关，设置操作及 Wi-Fi 轮询后刷新单调时间。
- test_settings_integration 直接包含真实 main.cpp，以 fake 存储/时钟/Wi-Fi 运行 setup、handleKey、handleConnection、loop 和 draw；覆盖离线编辑、Fn/Alt 分派、切页/断线保持、测试取得 IP 后释放、静置无文件轮询/重新连接、保存周期不取消在途查询或打开已关闭自动任务。
- 初轮集成用例未在 BLE 模拟断开后重新连接就断言 Codex 已发出请求；修正测试前置条件后重跑，无剩余失败。
- 定向命令：`pio test -d firmware -e native -f test_settings_controller -f test_settings_page -f test_settings_integration -f test_codex_config -f test_codex_refresh -f test_input_router -f test_scripts_integration`，28 个用例无失败。随后补充外部释放重绘/独立保存反馈回归，重跑三个设置套件，11 个用例无失败；本轮合计覆盖 29 个不同用例。
- 命令使用 `/Users/naaran/.platformio/penv/bin/pio`。首次 native 执行受沙箱外 PlatformIO 缓存锁权限阻断，随后按获准扩权执行；未调整依赖、锁文件或缓存权限。
- `pio run -d firmware` 完成编译和链接：RAM 74,564 / 327,680 字节（22.8%），Flash 2,089,909 / 3,342,336 字节（62.5%）。本轮编译用于主循环接线检查，未执行 task-09 的完整 native 回归。
- `git diff --check` 无空白错误。未刷写、未上传文件系统、未暂存/提交；保留原有暂存内容。真机操作和验收留 task-11。

## 2026-09-06 — task-09 完整回归与公共调用验证完成

- 用户授权执行 task-09；复核已有服务及设置接线，复用现有 fake 与真实 main.cpp 原生测试接缝，未新增生产联网模块、未修改生产代码。
- test_wifi_service 新增 2 个用例：模块配置检查 → Connecting → Connected → 超过连接截止时间仍持有 → 调用方显式 close → Off；关闭后跨长时间及毫秒回绕无重连、无多余关闭、无文件读取。覆盖启动失败、连接失败、超时和取消后的使用权释放、新请求可接受及旧身份不能关闭新请求。
- 扩展 test_settings_integration 的真实主循环场景：模块连接与设置共用唯一服务；设置扫描/测试返回 Busy，设置关闭重试不抢占；切页和 BLE 断线不结束模块连接；保存新配置不改变在途凭据，下一次连接读取最新值；显式关闭后模拟 24 小时，无重新连接或文件轮询。
- 完整命令：`/Users/naaran/.platformio/penv/bin/pio test -d firmware -e native`，28 个套件、111 个用例无失败，耗时 35.996 秒；公共 Wi-Fi 服务共 7 个用例，真实设置接线 1 个综合用例。无未处理自动化失败。
- 目标命令：`/Users/naaran/.platformio/penv/bin/pio run -d firmware`，增量构建完成，耗时 4.258 秒；RAM 74,564 / 327,680 字节（22.8%），Flash 2,089,909 / 3,342,336 字节（62.5%）。生产源码未变，复用已有编译产物；未升级依赖或改锁文件。
- 两条 PlatformIO 命令均使用获准的沙箱外用户缓存权限。`git diff --check` 无空白错误；未暂存/提交，保留既有暂存内容，未刷写或上传文件系统。
- Task 09 标记完成；Task 10 文档及 Task 11 真机验收尚未执行。fake 清理调用和构建不证明真实无线关闭、普通/公司认证、异步扫描停止、BLE 共存、中文像素、五档实际亮度或断电恢复，须后续设备证据和用户验收。

## 2026-09-07 — task-10 操作文档完成

- 用户授权继续 task-10、task-11；完成根 README 设置入口、关键按键和 LittleFS 首次部署说明，移除设置占位说明，详情集中在固件 README。
- 固件 README 补齐亮度/Codex/Wi-Fi 操作、明文字段与字节约束、保存/测试区别、AUTO 静默、完整值查看和关闭重试；更新主循环、输入优先级及设置已接入说明。
- 记录 WifiService 10 个公共接口名称和非阻塞调用片段，说明 requestId、Busy、过期身份、成功后显式关闭、关闭失败保留身份及查询不保证实际可连。
- 新增 acceptance.md，含 23 项验收步骤/预期/实际结果/证据栏；实际结果留空。核对当前源码接口、默认值、配置路径、按键及文档本地链接，检查无残留的设置占位说明；`git diff --check` 无空白错误。
- 本轮文档变更未重新构建/运行测试，沿用 task-09 的 111 个 native 用例及目标构建记录。未修改生产代码、未暂存/提交。

## 2026-09-07 — task-11 已准备，等待真机结果

- 验收表记录现有固件 bin SHA-256、环境记录栏及人工取证方法；未将本地产物摘要等同于设备已刷写版本。
- 本轮只读串口检查仅发现 Bluetooth-Incoming-Port 和 debug-console，没有可识别的 ADV USB 串口。已向用户询问本轮刷写及实测情况，目前尚无逐项设备结果。
- 现有串口 t 只诊断系统时间，没有 Wi-Fi 模式/故障注入接口；驱动 WIFI_OFF、模块真机持有连接和受控异常需要单独诊断环境。验收表明确相应限制，未新建诊断固件或执行刷写。
- task-11 保持未勾选。普通/公司认证、真实关闭及 BLE 共存、五档亮度、中文显示、断电恢复均待用户设备证据；未经实测不作验收完成结论。
