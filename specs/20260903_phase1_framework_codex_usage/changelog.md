# 变更记录

> 本文件按时间顺序追加，不覆盖既有记录。

## 2026-09-03

- Phase 1 需求已确认，首期默认以 macOS 作为电脑端开发与验收平台。
- 调整 Codex 查询职责：ADV 通过 `Enter` 或 Codex 页面前台 5 分钟定时主动发起，电脑端仅被动查询并响应；离开 Codex 页面后停止自动查询。
- Phase 2 增加用户提供的 `m5stickc-plus-pc-monitor` 参考实现：电脑端确定使用 Python 3.12 + asyncio + Bleak，并迁移其 macOS BLE 扫描、discovery、权限与重连经验。
- BLE 帧设计由长度前缀调整为已实践的 JSON Lines + 20 字节分片；新实现按 bytes 组装完整行后再 UTF-8 解码，并使用独立 UUID 避免与参考设备冲突。
- Phase 2 技术设计已确认：界面尽量使用中文；用户负责真机 UI 效果与电脑端实际运行/蓝牙/真实账号确认，Codex 负责自动化测试、构建检查和验证步骤交付。
- Phase 3 自检确认前期字体试验中的 `efontCN_16` 可显示基础中文，后续直接复用该基线，仅继续验证完整业务文案和资源表现；试验工程在正式能力迁移完成后移除。
- 用户明确要求执行 `task-01`，视为 Phase 3 任务拆解已确认。
- 完成 `task-01`：新增共享协议说明与契约 fixtures，固定独立 GATT UUID、协议版本、动作标识、结果代码及 hello/Codex 查询样例；静态 JSON 与契约一致性检查完成。
- 完成 `task-02`：建立 uv 管理的 Python 3.12 桌面工程、严格配置模型、Codex 安全默认值和动作级错误隔离；配置自动化用例共执行 10 项。
- 用户补充确认电脑端配置文件直接命名为 `desktop/config.json` 并提供默认配置；同步更新设计与 task-02 修改范围，后续 CLI 默认加载该文件。
- 完成 `task-03`：实现 JSONL 消息模型、bytes 分帧、异步路由和会话清理，并使用共享 fixtures 覆盖跨分片 UTF-8、边界错误及 execId 关联；自动化用例共执行 20 项。
- 完成 `task-04`：依据官方 Codex App Server 契约实现惰性 stdio 客户端、初始化握手、账号检查、RPC 关联和单/多额度桶转换；假进程自动化用例共执行 8 项，未访问真实账号。
- 完成 `task-05`：实现 Action Registry、完全由 ADV 请求驱动的 Codex 处理器及防御性 QueryGate；被动性、并发和错误映射自动化用例共执行 9 项。
- 完成 `task-06`：实现 Bleak Central 的服务优先扫描与回退、设备选择、有限 GATT 重试、原始 bytes notify、20 字节顺序写入、断线和有上限退避；最终假 Bleak 自动化用例共执行 9 项。
- 完成 `task-07`：装配配置、模块管理、hello、消息消费、BLE 重连和脱敏诊断，补充 uv 安装/启动文档；内存集成用例执行 5 项，完整桌面测试执行 61 项，CLI help 启动检查完成。真实 BLE 与 Codex 账号留待用户人工确认。
- 代码审查修复：集中桌面端协议常量并与共享 fixture 做契约校验；让 BLE 会话在查询期间继续接收并对第二请求返回 BUSY；激活空闲半包超时；将写失败、队列溢出和断线纳入诊断/重连；computerId 改为基于 macOS 平台 UUID 的脱敏稳定标识；精简未使用会话字段并完善 `.gitignore`。完整桌面测试增至 67 项。

## 2026-09-04

- 完成 `task-08`：建立正式 PlatformIO 固件工程和显示、键盘、单调时钟硬件适配入口；ESP32-S3 目标可构建。
- 完成 `task-09`：实现固件端消息 Codec、Router、协议常量及 hello 会话管理；协议 native 测试执行 6 项。
- 完成 `task-10`：实现 ESP32 GATT Server、回调有限队列、20 字节 notify 及 4 KiB JSONL bytes 缓冲；传输 native 测试执行 4 项。
- 完成 `task-11`：实现全局 Fn 导航、顶部栏、BLE 图标、断开提示及三个中文模块占位页；导航 native 测试执行 4 项。
- 完成 `task-12`：实现 Codex 用量状态、execId 互斥与仅前台刷新的调度策略；刷新 native 测试执行 7 项。
- 完成 `task-13`：实现中文 Codex 用量列表、进度条、相对时间、异常降级与滚动；页面 native 测试执行 5 项。
- Task 08～13 自动化回归共 26 项，ESP32-S3 最终构建使用 RAM 48,852 bytes（14.9%）、Flash 1,458,141 bytes（43.6%）；BLE、键盘和 UI 真机结果留待人工确认。
- 完成 `task-14`：新增项目总览、双端完整运行说明及一期人工验收清单；统一固件与桌面端广播名称回退，清理拒绝 hello 后的旧 Codex 缓存，并为长动态额度名称增加 UTF-8 安全截断。
- 全量自动化执行包括桌面 67 项、固件 27 项、uv 锁定依赖、协议 fixtures、CLI help、文档命令核对和 ESP32-S3 构建；最终 RAM 48,852 bytes（14.9%）、Flash 1,458,589 bytes（43.6%）。真实 BLE、真实 Codex 账号和真机 UI 继续由用户按清单确认。
- 参考官方 M5Cardputer UserDemo 完成 UI 视觉优化：保留横向顶部导航，将四个模块、BLE 与电池状态合并进荧光绿圆角顶栏，内容区改为炭灰背景、深灰卡片和荧光绿强调色，并保留橙/红额度告警；显示适配层继续使用字体层级、文本测量及 `LGFX_Sprite` 双缓冲，分配失败时自动退回直接绘制。固件 27 项 native 测试和 ESP32-S3 构建通过，最终 RAM 49,556 bytes（15.1%）、Flash 1,479,533 bytes（44.3%），真机颜色、中文字形、缓冲内存和视觉效果仍由用户确认。

- 提交前文档整理（2026-09-05）：用户反馈已按本地步骤完成 ADV 等检查；本地准备材料及独立验收清单不提交，正式文档改为引用 specs 中的验收步骤，反馈记录在 tasks.md，不补录未提供的测量数值。
