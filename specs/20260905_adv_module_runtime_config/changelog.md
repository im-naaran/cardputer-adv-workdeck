> 状态：草稿

# 变更记录

## 2026-09-05

- 用户确认刷新周期归 ADV，并要求使用 spec-driven-development 设计。
- 用户明确采用模块独立的设备运行时 JSON 文件；后续 settings 可修改并立即生效，希望按需读取最新值。
- 完成 Phase 0：核对现有调度器、控制器、hello 配置链路及缺少文件存储能力的现状。
- 完成 Phase 1 需求草稿；提出按需文件读取与保存后立即应用的语义，等待确认后进入技术设计。未修改生产代码或运行构建。
- 用户回复“继续”，确认 Phase 1 需求，requirements 状态更新为已确认。
- 完成 Phase 2 技术设计草稿：LittleFS 模块文件、首次 uploadfs 部署、按需读取与保存后立即应用、临时文件替换及失败边界、本机串口配置入口、桌面废弃字段迁移和超时默认统一。核对本地板卡默认 8MB 分区及 LittleFS API；未构建、刷写或修改生产代码，等待设计确认。
- 用户回复“继续”，确认 Phase 2 技术设计，design 状态更新为已确认。
- 完成 Phase 3 任务草稿，共 8 项：配置类型、LittleFS 存储、配置服务、双端周期链路清理、串口/主循环接线、集成回归、文档、独立真机验收；每项包含依赖和验证边界。未修改生产代码或运行构建，等待任务确认。
- 用户回复“继续”，确认 Phase 3 任务拆解，进入 Phase 4 实现。
- 完成 task-01：配置严格解析、512-byte 限制、尾部校验、默认镜像样例已实现；最终 test_codex_config 4 项无失败。采用轻量读取器避免引入标准 iostream/locale。
- 完成 task-02：LittleFS 原分区挂载、有界 stdio/VFS 读取、临时写入核对及 rename 替换已实现；真实临时目录用例注入部分写入/内容损坏/rename 失败，旧文件保留。缺失、目录、超长与未挂载有断言；底层 LittleFS 断电和真实短读尚未实测，归设备边界。buildfs 已生成 /config/codex.json，未上传。
- 完成 task-03：read/reload/save 及 Controller.applyConfig 已实现；每次读取、同值不写/不延后、失败恢复、暂停、回绕及 I/O 后取时已有断言。串口集成另验证在途响应保留和实际截止时间。
- 完成 task-04：双端周期字段已清理，旧桌面键忽略并提示；RPC 默认统一。删除前对旧 Codec 的无 settings 默认 300 秒断言已运行且无失败；删除后新旧 hello 均覆盖。本轮桌面全量 98 项无失败，未运行旧设备二进制。
- 完成 task-05：启动挂载/加载、三个串口命令、行缓冲/每轮字节预算、t 兼容、置脏与 I/O 后主循环取时已接入；test_config_commands 1 项复合场景无失败。
- 完成 task-06：最终固件全量 native 62 项、桌面全量 98 项均无失败；双任务集成新增本地配置与旧 hello 共存场景。固件目标构建及 LittleFS 镜像构建完成，未 upload/uploadfs。
- 完成 task-07：根/三子目录 README 已更新，提供本地配置验收清单（不随仓库提交，步骤见 task-08）；链接、协议 JSON 与默认样例检查无报错，git diff --check 无报错。
- task-08：未运行 upload/uploadfs/erase 或真机验收，任务保持未勾选；设备操作与记录模板已提供。
- 最终目标构建完成：Flash 1540089 bytes / RAM 50540 bytes；LittleFS 镜像 1572864 bytes，包含 /config/codex.json。移除标准字符串流后避免额外 iostream/locale 开销。最终差异空白检查无报错，未进行 Git 暂存或提交。

- 提交前文档整理（2026-09-05）：用户反馈已按本地步骤完成 ADV 等检查；本地准备材料及独立验收清单不提交，正式文档改为引用 specs 中的验收步骤，反馈记录在 tasks.md，不补录未提供的测量数值。
